package priorityQueuesp1;

public class Demo {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
//PriorityQueue thePQ = new PriorityQueue(5);
//thePQ.insert(30);
//thePQ.insert(10);
//thePQ.insert(20);
//thePQ.insert(70);
//thePQ.insert(80);
//link list: if sorted: big o is n to insert;
//if not sorted: big o is 1; 
//if sorted; to find the value big o is logn; to remove big o is n; total n;
//if not sorted: to find the value big o is n, to remove big o is 1;  total n;
//while(!thePQ.isEmpty()) {
//	System.out.println(thePQ.remove() + " ");
//}

PriorityQueue pq = new PriorityQueue();
	}
}
